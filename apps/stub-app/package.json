{
  "name": "@andromeda-eng/starbase-stub-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "echo \"No app tests yet\""
  },
  "dependencies": {
    "@andromeda-eng/starbase-stub-package": "workspace:*"
  }
}
